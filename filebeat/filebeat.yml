filebeat.inputs:
  - type: container
    paths:
      - /var/lib/docker/containers/*/*.log
      - /var/log/syslog
      - /var/log/*.log

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]
  username: "elastic"
  password: "changeme"  

setup.kibana:
  host: "http://kibana:5601"
